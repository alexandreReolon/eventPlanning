import { StyleSheet } from 'react-native';
import colors from '../../colors'

export default StyleSheet.create({
    text: {
        color: colors.text,
        fontSize: 20,
        fontWeight: 'bold'
    }
});